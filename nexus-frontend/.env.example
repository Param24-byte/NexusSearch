# NexusSearch Frontend — Environment Variables
# Copy to .env.local — never commit .env.local to git

# Backend API URL
# Development: http://localhost:8000
# Production: https://your-nexus-backend.railway.app
# Leave empty to run in DEMO MODE (uses mock data, no backend needed)
NEXT_PUBLIC_API_URL=http://localhost:8000
